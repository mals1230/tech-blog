<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{description}}</p>
  <p>Posted by {{user.name}} on {{format_date date_created}}</p>

  <p>
    {{#each posts as |post|}}
    <span class="posts">{{post.userComment}}</span>
    <p> Posted by {{Post.user.name}} on {{format_date Post.date_created}}</p>
    {{/each}}
  </p>

    <form class="form new-post-form">
      <div class="form-group">
        <label for="comment-form">Comment:</label>
        <input class="form-input" type="text" id="#comment-form" name="#comment-form" />
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Submit</button>
      </div>
    </form>

</div>
<script src="/js/posts.js"></script>